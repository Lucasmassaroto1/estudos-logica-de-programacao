/**
 * =========Tipos de dados e Conceitos Básicos=========
 * Variáveis
 * - Let, Const, Var
 * Tipos de dados
 * - String, Number("Number(), parseInt(), parseFloat()"), Boolean(True ou False), Nulo, Undefined
 * Operadores
 * - Adição(+), Subtração(-), Multiplicação(*), Divisão(/), Resto da divisão(%)
 * Operadores de Atribuição
 * - =, +=, -=, *=, /=, %=, **=
 * Operadores de Incremento e Decremento
 * - ++, --
 * Condição
 * - If, Else if, Else
 * Laços de repetição(Loop)
 * Funções
 * - Bloco reutilizáveis de Código
 */
